<div class="page-header">
  <h1>Posts by <%= @author_display_name %></h1>
</div>

<%= render :partial => "post", :collection => @posts %>

<%= paginate @posts %>

<%= link_to('RSS FEED', author_path(@author, :format=>:rss)) %>
