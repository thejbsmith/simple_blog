<div class="page-header">
  <h1>Posts tagged with <%= @tag %></h1>
</div>

<%= render :partial => "post", :collection => @posts %>

<%= paginate @posts %>

<%= link_to('RSS FEED', tag_path(@tag, :format=>:rss)) %>
