<div class="page-header">
  <h1><%= @category.name %></h1>
</div>

<%= render :partial => "post", :collection => @posts %>

<%= paginate @posts %>

<%= link_to('RSS FEED', category_path(@category.slug, :format=>:rss)) %>
